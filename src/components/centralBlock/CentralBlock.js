import React from 'react'
import './CentralBlock.scss'

export const CentralBlock = ({children})=>{
    return(
        <div className='container wrapper_inner'>
            {children}

        </div>
    )
}